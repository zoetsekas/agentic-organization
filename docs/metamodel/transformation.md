# The transformation, specified

Generated by `orgagents metamodel transformation` from `orgagents/metamodel/transformation.py` (ADR-0104). What every model element becomes in the IR, and where every link is carried. `orgagents metamodel trace <spec>` checks a compile against it, both ways.

## Elements

| Stereotype | Becomes | In the IR |
|---|---|---|
| «System» | collection | — — the SystemIR itself |
| «Organization» | collection | teams — the root TeamIR |
| «Team» | collection | teams |
| «Agent» | collection | agents |
| «SubAgent» | owned | subagents — a SubAgentIR in its agent's image |
| «Person» | collection | people |
| «Role» | resolved | each player's role_ids, capabilities and permissions — a role grants; the IR holds what was granted |
| «Decision» | resolved | the mandate of each unit and agent that holds it (MandateIR) |
| «Separation» | collection | separations |
| «Policy» | collection | policies |
| «Capability» | collection | capabilities |
| «DataClass» | collection | data_classes |
| «Environment» | collection | environments |
| «Endpoint» | per_holder | endpoints |
| «Mission» | collection | missions |
| «Workflow» | collection | workflows |
| «Trigger» | collection | triggers |
| «Channel» | collection | channels |
| «Knowledge» | collection | knowledge |
| «MemoryNamespace» | collection | memory.namespaces |
| «Guardrail» | collection | guardrails |
| «OutputContract» | per_holder | output_contract |
| «Evaluation» | collection | lifecycle.evaluations |
| «Skill» | per_holder | skills |
| «Plugin» | resolved | each holder's skills and tools (what it provides) and its id in AgentIR.plugins — a package, unpacked |
| «Tool» | per_holder | tools |
| «Action» | owned | graph.nodes — a step of its workflow |
| «ControlFlow» | owned | graph.edges — an edge of its workflow |
| «Note» | design_only | — — a comment on the canvas |

## Derived in the IR

| IR collection | From |
|---|---|
| placements | one per (placement unit, environment) with agents deployed there (ADR-0069, ADR-0082) |
| identities | one per agent: the principal it runs as |
| resources | the platform resources the design needs |
| placement_rules | the placement boundary each unit falls in |

## Links carried under another name

| Relationship | IR field |
|---|---|
| team.teams | child_team_ids |
| team.members | member_ids |
| team.leader | leader_agent_id |
| agent.successor | successor_agent_id |
| agent.roles | role_ids |
| agent.humans | humans |
| trigger.agent | agent_id |
| agent.data_dependencies | data_dependencies |

## Links resolved rather than carried

| Relationship | How it is checked |
|---|---|
| role.capabilities | every capability a role grants is among each player's capabilities, less what it withholds |
| separation.decisions | carried on the separation; enforced over each agent's resolved mandate |
| team.roles | a unit's roles become its permissions, inherited by its sub-teams, and their capabilities reach every member (checked with each agent's effective set) |
| plugin.provides_skills | every skill a plugin provides is among each holder's skills |
| plugin.provides_tools | every tool a plugin provides is among each holder's tools |
| plugin.requires_capabilities | every capability a plugin requires is held by each holder |

Every other relationship is carried under its own field name on its owner's image.

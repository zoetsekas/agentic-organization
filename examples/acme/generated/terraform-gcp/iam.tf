# Generated by orgagents from the system spec 'acme' (spec_version 1.3.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_service_account" "ceo" {
  # Workload identity for agent 'ceo' (ADR-0015)
  account_id   = "agent-ceo"
  display_name = "ceo-agent workload identity"
}

resource "google_project_iam_member" "ceo_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.ceo.email}"
}

resource "google_project_iam_member" "ceo_roles_run_invoker" {
  # delegate:agent:*
  project = var.project
  role    = "roles/run.invoker"
  member  = "serviceAccount:${google_service_account.ceo.email}"
}

resource "google_service_account" "cfo" {
  # Workload identity for agent 'cfo' (ADR-0015)
  account_id   = "agent-cfo"
  display_name = "cfo-agent workload identity"
}

resource "google_project_iam_member" "cfo_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.cfo.email}"
}

resource "google_project_iam_member" "cfo_roles_bigquery_dataviewer" {
  # query:capability:warehouse_query
  project = var.project
  role    = "roles/bigquery.dataViewer"
  member  = "serviceAccount:${google_service_account.cfo.email}"
}

resource "google_project_iam_member" "cfo_roles_run_invoker" {
  # delegate:agent:*
  project = var.project
  role    = "roles/run.invoker"
  member  = "serviceAccount:${google_service_account.cfo.email}"
}

resource "google_project_iam_member" "cfo_roles_editor" {
  # write:data_class:public_knowledge
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.cfo.email}"
}

resource "google_secret_manager_secret" "cfo_wiki_token" {
  # Secret is referenced, never valued, by the generator (ADR-0015)
  secret_id = "WIKI_TOKEN"
}

resource "google_service_account" "analyst" {
  # Workload identity for agent 'analyst' (ADR-0015)
  account_id   = "agent-analyst"
  display_name = "financial-analyst-agent workload identity"
}

resource "google_project_iam_member" "analyst_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.analyst.email}"
}

resource "google_project_iam_member" "analyst_roles_bigquery_dataviewer" {
  # query:capability:warehouse_query
  project = var.project
  role    = "roles/bigquery.dataViewer"
  member  = "serviceAccount:${google_service_account.analyst.email}"
}

resource "google_project_iam_member" "analyst_roles_editor" {
  # write:data_class:public_knowledge
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.analyst.email}"
}

resource "google_project_iam_member" "analyst_roles_run_invoker" {
  # invoke:workflow:governed_data_request
  project = var.project
  role    = "roles/run.invoker"
  member  = "serviceAccount:${google_service_account.analyst.email}"
}

resource "google_secret_manager_secret" "analyst_research_desk_key" {
  # Secret is referenced, never valued, by the generator (ADR-0015)
  secret_id = "RESEARCH_DESK_KEY"
}

resource "google_secret_manager_secret" "analyst_warehouse_dsn" {
  # Secret is referenced, never valued, by the generator (ADR-0015)
  secret_id = "WAREHOUSE_DSN"
}

resource "google_secret_manager_secret" "analyst_wiki_token" {
  # Secret is referenced, never valued, by the generator (ADR-0015)
  secret_id = "WIKI_TOKEN"
}

resource "google_service_account" "reconciler" {
  # Workload identity for agent 'reconciler' (ADR-0015)
  account_id   = "agent-reconciler"
  display_name = "reconciliation-agent workload identity"
}

resource "google_project_iam_member" "reconciler_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.reconciler.email}"
}

resource "google_project_iam_member" "reconciler_roles_bigquery_dataviewer" {
  # query:capability:warehouse_query
  project = var.project
  role    = "roles/bigquery.dataViewer"
  member  = "serviceAccount:${google_service_account.reconciler.email}"
}

resource "google_secret_manager_secret" "reconciler_customer_master_dsn" {
  # Secret is referenced, never valued, by the generator (ADR-0015)
  secret_id = "CUSTOMER_MASTER_DSN"
}

resource "google_service_account" "cto" {
  # Workload identity for agent 'cto' (ADR-0015)
  account_id   = "agent-cto"
  display_name = "cto-agent workload identity"
}

resource "google_project_iam_member" "cto_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.cto.email}"
}

resource "google_project_iam_member" "cto_roles_run_invoker" {
  # delegate:agent:*
  project = var.project
  role    = "roles/run.invoker"
  member  = "serviceAccount:${google_service_account.cto.email}"
}

resource "google_project_iam_member" "cto_roles_editor" {
  # write:data_class:public_knowledge
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.cto.email}"
}

resource "google_service_account" "platform_lead" {
  # Workload identity for agent 'platform_lead' (ADR-0015)
  account_id   = "agent-platform_lead"
  display_name = "platform-lead-agent workload identity"
}

resource "google_project_iam_member" "platform_lead_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.platform_lead.email}"
}

resource "google_project_iam_member" "platform_lead_roles_run_invoker" {
  # delegate:agent:*
  project = var.project
  role    = "roles/run.invoker"
  member  = "serviceAccount:${google_service_account.platform_lead.email}"
}

resource "google_project_iam_member" "platform_lead_roles_editor" {
  # write:data_class:public_knowledge
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.platform_lead.email}"
}

resource "google_service_account" "platform_engineer" {
  # Workload identity for agent 'platform_engineer' (ADR-0015)
  account_id   = "agent-platform_engineer"
  display_name = "platform-engineer-agent workload identity"
}

resource "google_project_iam_member" "platform_engineer_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.platform_engineer.email}"
}

resource "google_project_iam_member" "platform_engineer_roles_editor" {
  # write:capability:code_change
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.platform_engineer.email}"
}

resource "google_secret_manager_secret" "platform_engineer_repo_token" {
  # Secret is referenced, never valued, by the generator (ADR-0015)
  secret_id = "REPO_TOKEN"
}

resource "google_service_account" "sre" {
  # Workload identity for agent 'sre' (ADR-0015)
  account_id   = "agent-sre"
  display_name = "sre-agent workload identity"
}

resource "google_project_iam_member" "sre_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.sre.email}"
}

resource "google_project_iam_member" "sre_roles_pubsub_publisher" {
  # publish:channel:incidents
  project = var.project
  role    = "roles/pubsub.publisher"
  member  = "serviceAccount:${google_service_account.sre.email}"
}

resource "google_service_account" "cro" {
  # Workload identity for agent 'cro' (ADR-0015)
  account_id   = "agent-cro"
  display_name = "cro-agent workload identity"
}

resource "google_project_iam_member" "cro_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.cro.email}"
}

resource "google_project_iam_member" "cro_roles_run_invoker" {
  # delegate:agent:*
  project = var.project
  role    = "roles/run.invoker"
  member  = "serviceAccount:${google_service_account.cro.email}"
}

resource "google_project_iam_member" "cro_roles_editor" {
  # write:data_class:public_knowledge
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.cro.email}"
}

resource "google_secret_manager_secret" "cro_research_desk_key" {
  # Secret is referenced, never valued, by the generator (ADR-0015)
  secret_id = "RESEARCH_DESK_KEY"
}

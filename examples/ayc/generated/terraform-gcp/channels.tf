# Generated by orgagents from the system spec 'ayc' (spec_version 1.3.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_cloud_run_v2_service" "ops_desk" {
  # Where stock and fulfillment alerts land for humans.
  # provider=google_chat purposes=notify
  # sla=none out_of_hours=queue
  # escalation: none
  name             = "channel-ops_desk"
  region = var.region
}

resource "google_secret_manager_secret" "ops_desk_credential" {
  # Workspace credential for 'ops_desk', referenced by name only.
  secret_id = "CHAT_BOT_TOKEN"
}

resource "google_cloud_run_v2_service" "growth_desk" {
  # Where listing and promotion questions go for a human.
  # provider=google_chat purposes=ask, notify
  # sla=none out_of_hours=queue
  # escalation: none
  name             = "channel-growth_desk"
  region = var.region
}

resource "google_secret_manager_secret" "growth_desk_credential" {
  # Workspace credential for 'growth_desk', referenced by name only.
  secret_id = "CHAT_BOT_TOKEN"
}

resource "google_firestore_database" "long_term_memory" {
  # Long-term agent memory (ADR-0028); namespaces: merchandising_lessons
  # retention: 365 days,
  # promotion from a session: True
  name             = "ayc-memory"
  region = var.region
}

resource "google_discovery_engine_data_store" "product_specs" {
  # Grounding source 'product_specs' (document_store); citation required: True
  # data classes: public_knowledge
  name             = "knowledge-product_specs"
  region = var.region
}

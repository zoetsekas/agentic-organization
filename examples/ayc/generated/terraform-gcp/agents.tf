# Generated by orgagents from the system spec 'ayc' (spec_version 1.4.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_cloud_run_v2_service" "ceo_agent" {
  name     = "agent-ceo_agent"
  region = var.region

  # team: AYC
  # reports to: no one
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.ceo_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "ceo_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "ayc"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "coo_agent" {
  name     = "agent-coo_agent"
  region = var.region

  # team: AYC / Operations
  # reports to: ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.coo_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "coo_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "operations"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "buyer_agent" {
  name     = "agent-buyer_agent"
  region = var.region

  # team: AYC / Operations / Purchasing
  # reports to: coo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.buyer_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "buyer_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "purchasing"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "buyer_agent_sandbox" {
  # Execution environment 'warehouse_ops' — tier medium, network allowlist,
  # timeout 300s, mounts: inventory_data, order_data, supplier_data
  name     = "env-buyer_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.ayc_vpc.id
          subnetwork = google_compute_subnetwork.purchasing__warehouse_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "warehouse_agent" {
  name     = "agent-warehouse_agent"
  region = var.region

  # team: AYC / Operations / Warehouse and Shipping
  # reports to: coo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.warehouse_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "warehouse_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "warehouse"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "warehouse_agent_sandbox" {
  # Execution environment 'warehouse_ops' — tier medium, network allowlist,
  # timeout 300s, mounts: inventory_data, order_data, supplier_data
  name     = "env-warehouse_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.ayc_vpc.id
          subnetwork = google_compute_subnetwork.warehouse__warehouse_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "inventory_agent" {
  name     = "agent-inventory_agent"
  region = var.region

  # team: AYC / Operations / Inventory Audit
  # reports to: coo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.inventory_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "inventory_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "inventory_audit"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "inventory_agent_sandbox" {
  # Execution environment 'warehouse_ops' — tier medium, network allowlist,
  # timeout 300s, mounts: inventory_data, order_data, supplier_data
  name     = "env-inventory_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.ayc_vpc.id
          subnetwork = google_compute_subnetwork.inventory_audit__warehouse_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "ap_agent" {
  name     = "agent-ap_agent"
  region = var.region

  # team: AYC / Operations / Finance
  # reports to: coo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 2 instance(s) × 1 session(s) = 2 concurrent (declared by the design)
  scaling {
    min_instance_count = 0
    max_instance_count = 2
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.ap_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "ap_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "finance"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "ap_agent_sandbox" {
  # Execution environment 'finance_ops' — tier minimal, network allowlist,
  # timeout 300s, mounts: financial_records
  name     = "env-ap_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.ayc_vpc.id
          subnetwork = google_compute_subnetwork.finance__finance_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "ar_agent" {
  name     = "agent-ar_agent"
  region = var.region

  # team: AYC / Operations / Finance
  # reports to: ap_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.ar_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "ar_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "finance"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "ar_agent_sandbox" {
  # Execution environment 'finance_ops' — tier minimal, network allowlist,
  # timeout 300s, mounts: financial_records
  name     = "env-ar_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.ayc_vpc.id
          subnetwork = google_compute_subnetwork.finance__finance_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "software_agent" {
  name     = "agent-software_agent"
  region = var.region

  # team: AYC / Operations / IT
  # reports to: coo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.software_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "software_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "it"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "software_agent_sandbox" {
  # Execution environment 'build' — tier medium, network allowlist,
  # timeout 300s, mounts: public_knowledge
  name     = "env-software_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.ayc_vpc.id
          subnetwork = google_compute_subnetwork.it__build.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "cgo_agent" {
  name     = "agent-cgo_agent"
  region = var.region

  # team: AYC / Growth
  # reports to: ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.cgo_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "cgo_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "growth"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "ecommerce_agent" {
  name     = "agent-ecommerce_agent"
  region = var.region

  # team: AYC / Growth / E-Commerce and Web
  # reports to: cgo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: 1 warm, at most 10 instance(s) × 4 session(s) = 40 concurrent (declared by the design)
  scaling {
    min_instance_count = 1
    max_instance_count = 10
    max_instance_request_concurrency = 4
  }
  template {
    service_account = google_service_account.ecommerce_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "ecommerce_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "ecommerce"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "ecommerce_agent_sandbox" {
  # Execution environment 'storefront_ops' — tier medium, network allowlist,
  # timeout 300s, mounts: order_data, customer_pii, product_content
  name     = "env-ecommerce_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.ayc_vpc.id
          subnetwork = google_compute_subnetwork.ecommerce__storefront_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "cs_agent" {
  name     = "agent-cs_agent"
  region = var.region

  # team: AYC / Growth / Customer Service
  # reports to: cgo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.cs_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "cs_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "customer_service"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "cs_agent_sandbox" {
  # Execution environment 'storefront_ops' — tier medium, network allowlist,
  # timeout 300s, mounts: order_data, customer_pii, product_content
  name     = "env-cs_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.ayc_vpc.id
          subnetwork = google_compute_subnetwork.customer_service__storefront_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "marketing_agent" {
  name     = "agent-marketing_agent"
  region = var.region

  # team: AYC / Growth / Marketing
  # reports to: cgo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.marketing_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "marketing_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "marketing"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "marketing_agent_sandbox" {
  # Execution environment 'studio' — tier minimal, network allowlist,
  # timeout 300s, mounts: product_content
  name     = "env-marketing_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.ayc_vpc.id
          subnetwork = google_compute_subnetwork.marketing__studio.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

# Generated by orgagents from the system spec 'ayc' (spec_version 1.4.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_compute_network" "ayc_vpc" {
  name                    = "vpc"
  auto_create_subnetworks = false
  # One VPC for the whole system. Every placement gets its own
  # subnet below, and nothing crosses without a firewall rule the
  # design asked for (ADR-0084).
}

resource "google_compute_subnetwork" "customer_service__storefront_ops" {
  name          = "subnet-customer_service--storefront_ops"
  region = var.region
  network       = google_compute_network.ayc_vpc.id
  ip_cidr_range = "10.8.0.0/24"
  # placement 'customer_service--storefront_ops' -- unit 'customer_service', sandbox 'storefront_ops',
  # posture 'allowlist', agents: cs_agent
}

resource "google_compute_subnetwork" "ecommerce__storefront_ops" {
  name          = "subnet-ecommerce--storefront_ops"
  region = var.region
  network       = google_compute_network.ayc_vpc.id
  ip_cidr_range = "10.9.0.0/24"
  # placement 'ecommerce--storefront_ops' -- unit 'ecommerce', sandbox 'storefront_ops',
  # posture 'allowlist', agents: ecommerce_agent
}

resource "google_compute_subnetwork" "ecommerce__warehouse_ops" {
  name          = "subnet-ecommerce--warehouse_ops"
  region = var.region
  network       = google_compute_network.ayc_vpc.id
  ip_cidr_range = "10.10.0.0/24"
  # placement 'ecommerce--warehouse_ops' -- unit 'ecommerce', sandbox 'warehouse_ops',
  # posture 'allowlist', agents: ecommerce_agent
}

resource "google_compute_subnetwork" "finance__finance_ops" {
  name          = "subnet-finance--finance_ops"
  region = var.region
  network       = google_compute_network.ayc_vpc.id
  ip_cidr_range = "10.11.0.0/24"
  # placement 'finance--finance_ops' -- unit 'finance', sandbox 'finance_ops',
  # posture 'allowlist', agents: ap_agent, ar_agent
}

resource "google_compute_subnetwork" "inventory_audit__warehouse_ops" {
  name          = "subnet-inventory_audit--warehouse_ops"
  region = var.region
  network       = google_compute_network.ayc_vpc.id
  ip_cidr_range = "10.12.0.0/24"
  # placement 'inventory_audit--warehouse_ops' -- unit 'inventory_audit', sandbox 'warehouse_ops',
  # posture 'allowlist', agents: inventory_agent
}

resource "google_compute_subnetwork" "it__build" {
  name          = "subnet-it--build"
  region = var.region
  network       = google_compute_network.ayc_vpc.id
  ip_cidr_range = "10.13.0.0/24"
  # placement 'it--build' -- unit 'it', sandbox 'build',
  # posture 'allowlist', agents: software_agent
}

resource "google_compute_subnetwork" "marketing__studio" {
  name          = "subnet-marketing--studio"
  region = var.region
  network       = google_compute_network.ayc_vpc.id
  ip_cidr_range = "10.14.0.0/24"
  # placement 'marketing--studio' -- unit 'marketing', sandbox 'studio',
  # posture 'allowlist', agents: marketing_agent
}

resource "google_compute_subnetwork" "purchasing__warehouse_ops" {
  name          = "subnet-purchasing--warehouse_ops"
  region = var.region
  network       = google_compute_network.ayc_vpc.id
  ip_cidr_range = "10.15.0.0/24"
  # placement 'purchasing--warehouse_ops' -- unit 'purchasing', sandbox 'warehouse_ops',
  # posture 'allowlist', agents: buyer_agent
}

resource "google_compute_subnetwork" "warehouse__warehouse_ops" {
  name          = "subnet-warehouse--warehouse_ops"
  region = var.region
  network       = google_compute_network.ayc_vpc.id
  ip_cidr_range = "10.16.0.0/24"
  # placement 'warehouse--warehouse_ops' -- unit 'warehouse', sandbox 'warehouse_ops',
  # posture 'allowlist', agents: warehouse_agent
}

resource "google_compute_firewall" "deny_cross_placement" {
  name      = "deny-cross-placement"
  network   = google_compute_network.ayc_vpc.id
  priority  = 65534
  direction = "INGRESS"
  # Default-deny between placements (ADR-0069 rule 7). Only the
  # allow rules below open a path.
  deny { protocol = "all" }
  source_ranges = ["10.0.0.0/8"]
}

resource "google_compute_firewall" "allow_within_customer_service__storefront_ops" {
  name      = "allow-within-customer_service--storefront_ops"
  network   = google_compute_network.ayc_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'customer_service--storefront_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.cs_agent.email]
  target_service_accounts = [google_service_account.cs_agent.email]
}

resource "google_compute_firewall" "allow_within_ecommerce__storefront_ops" {
  name      = "allow-within-ecommerce--storefront_ops"
  network   = google_compute_network.ayc_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'ecommerce--storefront_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.ecommerce_agent.email]
  target_service_accounts = [google_service_account.ecommerce_agent.email]
}

resource "google_compute_firewall" "allow_within_ecommerce__warehouse_ops" {
  name      = "allow-within-ecommerce--warehouse_ops"
  network   = google_compute_network.ayc_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'ecommerce--warehouse_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.ecommerce_agent.email]
  target_service_accounts = [google_service_account.ecommerce_agent.email]
}

resource "google_compute_firewall" "allow_within_finance__finance_ops" {
  name      = "allow-within-finance--finance_ops"
  network   = google_compute_network.ayc_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'finance--finance_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.ap_agent.email, google_service_account.ar_agent.email]
  target_service_accounts = [google_service_account.ap_agent.email, google_service_account.ar_agent.email]
}

resource "google_compute_firewall" "allow_within_inventory_audit__warehouse_ops" {
  name      = "allow-within-inventory_audit--warehouse_ops"
  network   = google_compute_network.ayc_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'inventory_audit--warehouse_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.inventory_agent.email]
  target_service_accounts = [google_service_account.inventory_agent.email]
}

resource "google_compute_firewall" "allow_within_it__build" {
  name      = "allow-within-it--build"
  network   = google_compute_network.ayc_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'it--build' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.software_agent.email]
  target_service_accounts = [google_service_account.software_agent.email]
}

resource "google_compute_firewall" "allow_within_marketing__studio" {
  name      = "allow-within-marketing--studio"
  network   = google_compute_network.ayc_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'marketing--studio' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.marketing_agent.email]
  target_service_accounts = [google_service_account.marketing_agent.email]
}

resource "google_compute_firewall" "allow_within_purchasing__warehouse_ops" {
  name      = "allow-within-purchasing--warehouse_ops"
  network   = google_compute_network.ayc_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'purchasing--warehouse_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.buyer_agent.email]
  target_service_accounts = [google_service_account.buyer_agent.email]
}

resource "google_compute_firewall" "allow_within_warehouse__warehouse_ops" {
  name      = "allow-within-warehouse--warehouse_ops"
  network   = google_compute_network.ayc_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'warehouse--warehouse_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.warehouse_agent.email]
  target_service_accounts = [google_service_account.warehouse_agent.email]
}

resource "google_compute_firewall" "allow_0_customer_service__storefront_ops_to_finance__finance_ops" {
  name      = "allow-0"
  network   = google_compute_network.ayc_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared notify flow (via flow:cs_agent->ar_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.cs_agent.email]
  target_service_accounts = [google_service_account.ap_agent.email, google_service_account.ar_agent.email]
}

resource "google_compute_firewall" "allow_1_ecommerce__storefront_ops_to_inventory_audit__warehouse_ops" {
  name      = "allow-1"
  network   = google_compute_network.ayc_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared consult flow (via flow:ecommerce_agent->inventory_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.ecommerce_agent.email]
  target_service_accounts = [google_service_account.inventory_agent.email]
}

resource "google_compute_firewall" "allow_2_ecommerce__warehouse_ops_to_inventory_audit__warehouse_ops" {
  name      = "allow-2"
  network   = google_compute_network.ayc_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared consult flow (via flow:ecommerce_agent->inventory_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.ecommerce_agent.email]
  target_service_accounts = [google_service_account.inventory_agent.email]
}

resource "google_compute_firewall" "allow_3_marketing__studio_to_ecommerce__storefront_ops" {
  name      = "allow-3"
  network   = google_compute_network.ayc_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared consult flow (via flow:marketing_agent->ecommerce_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.marketing_agent.email]
  target_service_accounts = [google_service_account.ecommerce_agent.email]
}

resource "google_compute_firewall" "allow_4_marketing__studio_to_ecommerce__warehouse_ops" {
  name      = "allow-4"
  network   = google_compute_network.ayc_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared consult flow (via flow:marketing_agent->ecommerce_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.marketing_agent.email]
  target_service_accounts = [google_service_account.ecommerce_agent.email]
}

resource "google_compute_firewall" "allow_5_purchasing__warehouse_ops_to_finance__finance_ops" {
  name      = "allow-5"
  network   = google_compute_network.ayc_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared notify flow (via flow:buyer_agent->ap_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.buyer_agent.email]
  target_service_accounts = [google_service_account.ap_agent.email, google_service_account.ar_agent.email]
}

resource "google_compute_firewall" "allow_6_warehouse__warehouse_ops_to_inventory_audit__warehouse_ops" {
  name      = "allow-6"
  network   = google_compute_network.ayc_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared escalate flow (via flow:warehouse_agent->inventory_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.warehouse_agent.email]
  target_service_accounts = [google_service_account.inventory_agent.email]
}

# NOTE: sandboxes build, finance_ops, storefront_ops, studio, warehouse_ops declare a
# hostname egress allowlist. A firewall rule works on IP
# ranges and service accounts, not FQDNs, so the allowlist is
# enforced by an egress proxy / Cloud NAT with an FQDN policy,
# wired in overlays/ -- not by these rules (ADR-0084).

# Generated by orgagents from the system spec 'ayc' (spec_version 1.4.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

"""Workflow `listing_readiness` — listing-readiness

Before a product is listed, check its Fishbowl stock and assemble the listing from the spec sheet.

Generated from the design's process graph. The graph shape is
exact; every node body raises until the host implements it.
"""
from __future__ import annotations

from typing import Any

from langgraph.graph import END, START, StateGraph


State = dict[str, Any]


def stock(state: State) -> State:
    """tool: calls tool 'stock_check'."""
    raise NotImplementedError(
        "workflow node 'stock' (tool) is the host to implement"
    )


def draft(state: State) -> State:
    """agent: delegates to agent 'ecommerce_agent'."""
    raise NotImplementedError(
        "workflow node 'draft' (agent) is the host to implement"
    )


def build() -> Any:
    """Assemble the declared graph."""
    builder = StateGraph(State)
    builder.add_node("stock", stock)
    builder.add_node("draft", draft)
    builder.add_edge(START, "stock")
    builder.add_edge("stock", "draft")
    builder.add_edge("draft", END)
    return builder.compile()


graph = build()

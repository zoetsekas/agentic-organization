# Generated by orgagents from the system spec 'ayc' (spec_version 1.4.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

from deepagents import create_deep_agent, FilesystemPermission
from langchain_core.tools import StructuredTool
from ._backends import mcp_call

def software_deploy(**kwargs):
    """Deploy a change to a production system."""
    return mcp_call(
        "deploy_pipeline", "https://deploy.ayc.internal/mcp",
        "streamable_http", "software_deploy",
        secret_env=None,
        **kwargs,
    )

software_agent = create_deep_agent(
    model="anthropic:claude-sonnet-4-5",
    tools=[StructuredTool.from_function(func=software_deploy, name="software_deploy")],
    system_prompt="""You are software-engineer.
Builds and deploys platform changes.

## Your instructions
Build behind tests and deploy behind an approval. A change to the storefront or to Fishbowl integration is never a solo act; it goes through the release gate with a rollback ready.

## Accountability
- Build and deploy platform changes safely.  _(role: engineer)_

## Organization
- Team: AYC / Operations / IT
- Leader of: it
- Reports to: coo_agent
- May delegate to: no one
- Escalates to: coo_agent

## The people you answer to
- Clark (Chief Operating Officer) — owner, clark@ayc.example
- Philip (Chief Executive Officer) — approver, philip@ayc.example
- Always seek approval before: software_deploy

## Memory
- Session memory is yours for this session only.
- Long-term namespaces you may recall from: merchandising_lessons.
- Promote something into long-term memory only when it will be useful again; everything you happen to see is not a memory.

## Shared operating principles
- The stock number in Fishbowl and the listing on Shopify tell one story; keep them the same one.  _(from organization)_
- A control you route around is a control you do not have.  _(from organization)_

## Boundaries enforced on you
- no_pii_outbound: data_class → block
- no_cost_price_outbound: data_class → block
- no_credentials_out: secrets → block
- screen_inbound_messages: prompt_injection → escalate
  These are enforced outside you. Do not attempt to work around one; say what you needed instead.

## Operating rules
- Prefer delegating to a team member whose role covers the task.
- Use an encoded workflow for any process that must be auditable.
- Prefer a sub-agent for a bounded task you can describe as a tool.
- You may not widen your own access; ask your leader instead.""",
    memory=["merchandising_lessons"],
    permissions=[
        FilesystemPermission(operations=['read', 'write'], paths=['/workspace/**'], mode="allow"),
        FilesystemPermission(operations=['read', 'write'], paths=['/**'], mode="deny"),
    ],
    interrupt_on={"software_deploy": True},
)
# interrupt_on pauses for a human; the approver the design routes to is Philip (enforced by this platform's harness).

# Carried by the design and NOT by this graph. Nothing below is
# enforced by serving this graph on LangGraph Platform:
#   - mandate: may decide ['deploy_change']
#   - 2 resolved permissions
#   - 1 tool(s) gated by interrupt_on — the graph pauses for a human, but the paired approver who may resume it is this platform's model, not deepagents': ['software_deploy']
#   - 4 guardrail(s) — expressible as deepagents middleware in host code, never in this graph
#   - placement 'it--build' and its network policy
#   - sandbox 'build': its filesystem is carried as permissions, but the network posture (allowlist) is the infrastructure target's, not deepagents'
#   - 2 paired human(s), including the approver an interrupt would route to

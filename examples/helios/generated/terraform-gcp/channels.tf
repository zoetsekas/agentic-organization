# Generated by orgagents from the system spec 'helios' (spec_version 1.3.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_cloud_run_v2_service" "clinical_escalations" {
  # Where a screened document or a safety signal goes for a human.
  # provider=slack purposes=ask, notify
  # sla=none out_of_hours=queue
  # escalation: none
  name             = "channel-clinical_escalations"
  region = var.region
}

resource "google_secret_manager_secret" "clinical_escalations_credential" {
  # Workspace credential for 'clinical_escalations', referenced by name only.
  secret_id = "SLACK_BOT_TOKEN"
}

resource "google_cloud_run_v2_service" "exec_briefing" {
  # Where portfolio and spend alerts reach the executives.
  # provider=smtp purposes=notify
  # sla=none out_of_hours=queue
  # escalation: none
  name             = "channel-exec_briefing"
  region = var.region
}

resource "google_secret_manager_secret" "exec_briefing_credential" {
  # Workspace credential for 'exec_briefing', referenced by name only.
  secret_id = "SMTP_CREDENTIAL"
}

resource "google_firestore_database" "long_term_memory" {
  # Long-term agent memory (ADR-0028); namespaces: trial_lessons, manufacturing_lessons
  # retention: 365 days,
  # promotion from a session: True
  name             = "helios-memory"
  region = var.region
}

# Generated by orgagents from the system spec 'helios' (spec_version 1.3.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_compute_network" "helios_vpc" {
  name                    = "vpc"
  auto_create_subnetworks = false
  # One VPC for the whole system. Every placement gets its own
  # subnet below, and nothing crosses without a firewall rule the
  # design asked for (ADR-0084).
}

resource "google_compute_subnetwork" "bioinformatics__hpc" {
  name          = "subnet-bioinformatics--hpc"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.8.0.0/24"
  # placement 'bioinformatics--hpc' -- unit 'bioinformatics', sandbox 'hpc',
  # posture 'none', agents: bioinfo_agent
}

resource "google_compute_subnetwork" "bioinformatics__phi_enclave" {
  name          = "subnet-bioinformatics--phi_enclave"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.9.0.0/24"
  # placement 'bioinformatics--phi_enclave' -- unit 'bioinformatics', sandbox 'phi_enclave',
  # posture 'none', agents: bioinfo_agent
}

resource "google_compute_subnetwork" "biostatistics__clinical_analysis" {
  name          = "subnet-biostatistics--clinical_analysis"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.10.0.0/24"
  # placement 'biostatistics--clinical_analysis' -- unit 'biostatistics', sandbox 'clinical_analysis',
  # posture 'allowlist', agents: biostat_agent
}

resource "google_compute_subnetwork" "biostatistics__phi_enclave" {
  name          = "subnet-biostatistics--phi_enclave"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.11.0.0/24"
  # placement 'biostatistics--phi_enclave' -- unit 'biostatistics', sandbox 'phi_enclave',
  # posture 'none', agents: biostat_agent
}

resource "google_compute_subnetwork" "commercial__commercial_analysis" {
  name          = "subnet-commercial--commercial_analysis"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.12.0.0/24"
  # placement 'commercial--commercial_analysis' -- unit 'commercial', sandbox 'commercial_analysis',
  # posture 'allowlist', agents: commercial_agent
}

resource "google_compute_subnetwork" "discovery__discovery_lab" {
  name          = "subnet-discovery--discovery_lab"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.13.0.0/24"
  # placement 'discovery--discovery_lab' -- unit 'discovery', sandbox 'discovery_lab',
  # posture 'allowlist', agents: discovery_agent
}

resource "google_compute_subnetwork" "pharmacovigilance__safety_review" {
  name          = "subnet-pharmacovigilance--safety_review"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.14.0.0/24"
  # placement 'pharmacovigilance--safety_review' -- unit 'pharmacovigilance', sandbox 'safety_review',
  # posture 'allowlist', agents: pv_agent
}

resource "google_compute_subnetwork" "process_dev__gmp_ops" {
  name          = "subnet-process_dev--gmp_ops"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.15.0.0/24"
  # placement 'process_dev--gmp_ops' -- unit 'process_dev', sandbox 'gmp_ops',
  # posture 'allowlist', agents: process_agent
}

resource "google_compute_subnetwork" "quality__gmp_ops" {
  name          = "subnet-quality--gmp_ops"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.16.0.0/24"
  # placement 'quality--gmp_ops' -- unit 'quality', sandbox 'gmp_ops',
  # posture 'allowlist', agents: qa_agent
}

resource "google_compute_subnetwork" "regulatory__regulatory_review" {
  name          = "subnet-regulatory--regulatory_review"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.17.0.0/24"
  # placement 'regulatory--regulatory_review' -- unit 'regulatory', sandbox 'regulatory_review',
  # posture 'allowlist', agents: regulatory_agent
}

resource "google_compute_subnetwork" "trial_ops__clinical_analysis" {
  name          = "subnet-trial_ops--clinical_analysis"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.18.0.0/24"
  # placement 'trial_ops--clinical_analysis' -- unit 'trial_ops', sandbox 'clinical_analysis',
  # posture 'allowlist', agents: trial_manager_agent
}

resource "google_compute_subnetwork" "trial_ops__phi_enclave" {
  name          = "subnet-trial_ops--phi_enclave"
  region = var.region
  network       = google_compute_network.helios_vpc.id
  ip_cidr_range = "10.19.0.0/24"
  # placement 'trial_ops--phi_enclave' -- unit 'trial_ops', sandbox 'phi_enclave',
  # posture 'none', agents: trial_manager_agent
}

resource "google_compute_firewall" "deny_cross_placement" {
  name      = "deny-cross-placement"
  network   = google_compute_network.helios_vpc.id
  priority  = 65534
  direction = "INGRESS"
  # Default-deny between placements (ADR-0069 rule 7). Only the
  # allow rules below open a path.
  deny { protocol = "all" }
  source_ranges = ["10.0.0.0/8"]
}

resource "google_compute_firewall" "allow_within_bioinformatics__hpc" {
  name      = "allow-within-bioinformatics--hpc"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'bioinformatics--hpc' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.bioinfo_agent.email]
  target_service_accounts = [google_service_account.bioinfo_agent.email]
}

resource "google_compute_firewall" "allow_within_bioinformatics__phi_enclave" {
  name      = "allow-within-bioinformatics--phi_enclave"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'bioinformatics--phi_enclave' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.bioinfo_agent.email]
  target_service_accounts = [google_service_account.bioinfo_agent.email]
}

resource "google_compute_firewall" "allow_within_biostatistics__clinical_analysis" {
  name      = "allow-within-biostatistics--clinical_analysis"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'biostatistics--clinical_analysis' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.biostat_agent.email]
  target_service_accounts = [google_service_account.biostat_agent.email]
}

resource "google_compute_firewall" "allow_within_biostatistics__phi_enclave" {
  name      = "allow-within-biostatistics--phi_enclave"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'biostatistics--phi_enclave' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.biostat_agent.email]
  target_service_accounts = [google_service_account.biostat_agent.email]
}

resource "google_compute_firewall" "allow_within_commercial__commercial_analysis" {
  name      = "allow-within-commercial--commercial_analysis"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'commercial--commercial_analysis' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.commercial_agent.email]
  target_service_accounts = [google_service_account.commercial_agent.email]
}

resource "google_compute_firewall" "allow_within_discovery__discovery_lab" {
  name      = "allow-within-discovery--discovery_lab"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'discovery--discovery_lab' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.discovery_agent.email]
  target_service_accounts = [google_service_account.discovery_agent.email]
}

resource "google_compute_firewall" "allow_within_pharmacovigilance__safety_review" {
  name      = "allow-within-pharmacovigilance--safety_review"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'pharmacovigilance--safety_review' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.pv_agent.email]
  target_service_accounts = [google_service_account.pv_agent.email]
}

resource "google_compute_firewall" "allow_within_process_dev__gmp_ops" {
  name      = "allow-within-process_dev--gmp_ops"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'process_dev--gmp_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.process_agent.email]
  target_service_accounts = [google_service_account.process_agent.email]
}

resource "google_compute_firewall" "allow_within_quality__gmp_ops" {
  name      = "allow-within-quality--gmp_ops"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'quality--gmp_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.qa_agent.email]
  target_service_accounts = [google_service_account.qa_agent.email]
}

resource "google_compute_firewall" "allow_within_regulatory__regulatory_review" {
  name      = "allow-within-regulatory--regulatory_review"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'regulatory--regulatory_review' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.regulatory_agent.email]
  target_service_accounts = [google_service_account.regulatory_agent.email]
}

resource "google_compute_firewall" "allow_within_trial_ops__clinical_analysis" {
  name      = "allow-within-trial_ops--clinical_analysis"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'trial_ops--clinical_analysis' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.trial_manager_agent.email]
  target_service_accounts = [google_service_account.trial_manager_agent.email]
}

resource "google_compute_firewall" "allow_within_trial_ops__phi_enclave" {
  name      = "allow-within-trial_ops--phi_enclave"
  network   = google_compute_network.helios_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'trial_ops--phi_enclave' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.trial_manager_agent.email]
  target_service_accounts = [google_service_account.trial_manager_agent.email]
}

resource "google_compute_firewall" "allow_0_bioinformatics__hpc_to_discovery__discovery_lab" {
  name      = "allow-0"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared notify flow (via flow:bioinfo_agent->discovery_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.bioinfo_agent.email]
  target_service_accounts = [google_service_account.discovery_agent.email]
}

resource "google_compute_firewall" "allow_1_bioinformatics__phi_enclave_to_discovery__discovery_lab" {
  name      = "allow-1"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared notify flow (via flow:bioinfo_agent->discovery_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.bioinfo_agent.email]
  target_service_accounts = [google_service_account.discovery_agent.email]
}

resource "google_compute_firewall" "allow_2_process_dev__gmp_ops_to_quality__gmp_ops" {
  name      = "allow-2"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared escalate flow (via flow:process_agent->qa_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.process_agent.email]
  target_service_accounts = [google_service_account.qa_agent.email]
}

resource "google_compute_firewall" "allow_3_regulatory__regulatory_review_to_commercial__commercial_analysis" {
  name      = "allow-3"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared consult flow (via flow:regulatory_agent->commercial_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.regulatory_agent.email]
  target_service_accounts = [google_service_account.commercial_agent.email]
}

resource "google_compute_firewall" "allow_4_trial_ops__clinical_analysis_to_pharmacovigilance__safety_review" {
  name      = "allow-4"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared escalate flow (via flow:trial_manager_agent->pv_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.trial_manager_agent.email]
  target_service_accounts = [google_service_account.pv_agent.email]
}

resource "google_compute_firewall" "allow_5_trial_ops__phi_enclave_to_pharmacovigilance__safety_review" {
  name      = "allow-5"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared escalate flow (via flow:trial_manager_agent->pv_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.trial_manager_agent.email]
  target_service_accounts = [google_service_account.pv_agent.email]
}

resource "google_compute_firewall" "deny_egress_bioinformatics__hpc" {
  name      = "deny-egress-bioinformatics--hpc"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "EGRESS"
  # sandbox 'hpc' posture 'none': its agents reach
  # nothing outbound -- PII, MNPI or a live sample never calls home.
  deny { protocol = "all" }
  destination_ranges = ["0.0.0.0/0"]
  target_service_accounts = [google_service_account.bioinfo_agent.email]
}

resource "google_compute_firewall" "deny_egress_bioinformatics__phi_enclave" {
  name      = "deny-egress-bioinformatics--phi_enclave"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "EGRESS"
  # sandbox 'phi_enclave' posture 'none': its agents reach
  # nothing outbound -- PII, MNPI or a live sample never calls home.
  deny { protocol = "all" }
  destination_ranges = ["0.0.0.0/0"]
  target_service_accounts = [google_service_account.bioinfo_agent.email]
}

resource "google_compute_firewall" "deny_egress_biostatistics__phi_enclave" {
  name      = "deny-egress-biostatistics--phi_enclave"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "EGRESS"
  # sandbox 'phi_enclave' posture 'none': its agents reach
  # nothing outbound -- PII, MNPI or a live sample never calls home.
  deny { protocol = "all" }
  destination_ranges = ["0.0.0.0/0"]
  target_service_accounts = [google_service_account.biostat_agent.email]
}

resource "google_compute_firewall" "deny_egress_trial_ops__phi_enclave" {
  name      = "deny-egress-trial_ops--phi_enclave"
  network   = google_compute_network.helios_vpc.id
  priority  = 900
  direction = "EGRESS"
  # sandbox 'phi_enclave' posture 'none': its agents reach
  # nothing outbound -- PII, MNPI or a live sample never calls home.
  deny { protocol = "all" }
  destination_ranges = ["0.0.0.0/0"]
  target_service_accounts = [google_service_account.trial_manager_agent.email]
}

# NOTE: sandboxes clinical_analysis, commercial_analysis, discovery_lab, gmp_ops, regulatory_review, safety_review declare a
# hostname egress allowlist. A firewall rule works on IP
# ranges and service accounts, not FQDNs, so the allowlist is
# enforced by an egress proxy / Cloud NAT with an FQDN policy,
# wired in overlays/ -- not by these rules (ADR-0084).

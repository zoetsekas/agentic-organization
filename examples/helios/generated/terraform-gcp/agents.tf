# Generated by orgagents from the system spec 'helios' (spec_version 1.4.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_cloud_run_v2_service" "ceo_agent" {
  name     = "agent-ceo_agent"
  region = var.region

  # team: Helios Therapeutics
  # reports to: no one
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.ceo_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "ceo_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "helios"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "cso_agent" {
  name     = "agent-cso_agent"
  region = var.region

  # team: Helios Therapeutics / Research and Development
  # reports to: ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.cso_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "cso_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "rnd"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "discovery_agent" {
  name     = "agent-discovery_agent"
  region = var.region

  # team: Helios Therapeutics / Research and Development / Discovery
  # reports to: cso_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.discovery_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "discovery_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "discovery"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "discovery_agent_sandbox" {
  # Execution environment 'discovery_lab' — tier medium, network allowlist,
  # timeout 300s, mounts: research_ip
  name     = "env-discovery_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.helios_vpc.id
          subnetwork = google_compute_subnetwork.discovery__discovery_lab.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "bioinfo_agent" {
  name     = "agent-bioinfo_agent"
  region = var.region

  # team: Helios Therapeutics / Research and Development / Bioinformatics
  # reports to: cso_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.bioinfo_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "8"
          memory = "32Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "bioinfo_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "bioinformatics"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_job" "bioinfo_agent_sandbox" {
  # Execution environment 'hpc' — tier accelerated, network none,
  # timeout 300s, mounts: research_ip
  name     = "env-bioinfo_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.helios_vpc.id
          subnetwork = google_compute_subnetwork.bioinformatics__hpc.id
        }
        egress = "PRIVATE_RANGES_ONLY"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "cmo_agent" {
  name     = "agent-cmo_agent"
  region = var.region

  # team: Helios Therapeutics / Clinical Development
  # reports to: ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.cmo_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "cmo_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "clinical"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "trial_manager_agent" {
  name     = "agent-trial_manager_agent"
  region = var.region

  # team: Helios Therapeutics / Clinical Development / Trial Operations
  # reports to: cmo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.trial_manager_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "trial_manager_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "trial_ops"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "trial_manager_agent_sandbox" {
  # Execution environment 'clinical_analysis' — tier medium, network allowlist,
  # timeout 300s, mounts: trial_data, regulatory_dossier
  name     = "env-trial_manager_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.helios_vpc.id
          subnetwork = google_compute_subnetwork.trial_ops__clinical_analysis.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "biostat_agent" {
  name     = "agent-biostat_agent"
  region = var.region

  # team: Helios Therapeutics / Clinical Development / Biostatistics
  # reports to: cmo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.biostat_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "biostat_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "biostatistics"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "biostat_agent_sandbox" {
  # Execution environment 'clinical_analysis' — tier medium, network allowlist,
  # timeout 300s, mounts: trial_data, regulatory_dossier
  name     = "env-biostat_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.helios_vpc.id
          subnetwork = google_compute_subnetwork.biostatistics__clinical_analysis.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "pv_agent" {
  name     = "agent-pv_agent"
  region = var.region

  # team: Helios Therapeutics / Clinical Development / Pharmacovigilance
  # reports to: cmo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.pv_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "pv_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "pharmacovigilance"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "pv_agent_sandbox" {
  # Execution environment 'safety_review' — tier medium, network allowlist,
  # timeout 300s, mounts: adverse_events, regulatory_dossier
  name     = "env-pv_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.helios_vpc.id
          subnetwork = google_compute_subnetwork.pharmacovigilance__safety_review.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "vp_mfg_agent" {
  name     = "agent-vp_mfg_agent"
  region = var.region

  # team: Helios Therapeutics / Manufacturing
  # reports to: ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.vp_mfg_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "vp_mfg_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "manufacturing"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "process_agent" {
  name     = "agent-process_agent"
  region = var.region

  # team: Helios Therapeutics / Manufacturing / Process Development
  # reports to: vp_mfg_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.process_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "process_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "process_dev"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "process_agent_sandbox" {
  # Execution environment 'gmp_ops' — tier medium, network allowlist,
  # timeout 300s, mounts: batch_records
  name     = "env-process_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.helios_vpc.id
          subnetwork = google_compute_subnetwork.process_dev__gmp_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "qa_agent" {
  name     = "agent-qa_agent"
  region = var.region

  # team: Helios Therapeutics / Manufacturing / Quality Assurance
  # reports to: vp_mfg_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.qa_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "qa_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "quality"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "qa_agent_sandbox" {
  # Execution environment 'gmp_ops' — tier medium, network allowlist,
  # timeout 300s, mounts: batch_records
  name     = "env-qa_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.helios_vpc.id
          subnetwork = google_compute_subnetwork.quality__gmp_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "regulatory_agent" {
  name     = "agent-regulatory_agent"
  region = var.region

  # team: Helios Therapeutics / Regulatory Affairs
  # reports to: ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.regulatory_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "regulatory_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "regulatory"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "regulatory_agent_sandbox" {
  # Execution environment 'regulatory_review' — tier medium, network allowlist,
  # timeout 300s, mounts: regulatory_dossier
  name     = "env-regulatory_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.helios_vpc.id
          subnetwork = google_compute_subnetwork.regulatory__regulatory_review.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "commercial_agent" {
  name     = "agent-commercial_agent"
  region = var.region

  # team: Helios Therapeutics / Commercial
  # reports to: ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.commercial_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "commercial_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "commercial"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "commercial_agent_sandbox" {
  # Execution environment 'commercial_analysis' — tier medium, network allowlist,
  # timeout 300s, mounts: market_data
  name     = "env-commercial_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.helios_vpc.id
          subnetwork = google_compute_subnetwork.commercial__commercial_analysis.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

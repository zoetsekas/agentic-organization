# Generated by orgagents from the system spec 'atlas' (spec_version 1.3.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

from google.adk.agents import LlmAgent
from google.adk.tools import FunctionTool

def deal_advisory(**kwargs):
    """Work an advisory mandate against material non-public information."""
    # Supplied by the host at registration; the design names the tool, not its code.
    raise NotImplementedError("bind deal_advisory in host code")

def wall_crossing(**kwargs):
    """Record bringing a named person across the information barrier."""
    # Supplied by the host at registration; the design names the tool, not its code.
    raise NotImplementedError("bind wall_crossing in host code")

banker_agent = LlmAgent(
    name="banker_agent",
    model="gemini-2.5-pro",
    description="Advises mandates and keeps MNPI behind the wall.",
    instruction="""You are investment-banker.
Advises mandates and keeps MNPI behind the wall.

## Your instructions
Everything you touch on a live mandate is material non-public information; it stays in the deal room. You never trade, and you never hand a position to someone who does. Bringing a person across the wall is logged and authorized, never a favour.

## Accountability
- Advise mandates against deal information, and cross people over the wall only when authorized.  _(role: banker)_
- Keep material non-public information behind the wall.  _(role: banker)_

## Organization
- Team: Atlas Global / Corporate and Investment Bank / Investment Banking
- Leader of: banking
- Reports to: cib_head_agent
- May delegate to: no one
- Escalates to: cib_head_agent

## The people you answer to
- Sanjay Iyer (Head of Investment Banking) — owner, sanjay.iyer@atlas.example
- Beatriz Costa (Head of Internal Audit) — approver, beatriz.costa@atlas.example
- Always seek approval before: deal_advisory, wall_crossing

## Shared operating principles
- Evidence over conviction. An unsourced number does not leave the desk.  _(from organization)_
- A control you route around is a control you do not have.  _(from organization)_
- When a limit, a barrier or a hold is in doubt, it holds until it is lifted.  _(from organization)_

## Boundaries enforced on you
- no_pii_outbound: data_class → block
- no_mnpi_outbound: data_class → block
- no_aml_outbound: data_class → block
- no_credentials_out: secrets → block
- screen_inbound_messages: prompt_injection → escalate
  These are enforced outside you. Do not attempt to work around one; say what you needed instead.

## Operating rules
- Prefer delegating to a team member whose role covers the task.
- Use an encoded workflow for any process that must be auditable.
- Prefer a sub-agent for a bounded task you can describe as a tool.
- You may not widen your own access; ask your leader instead.""",
    generate_content_config={
        "temperature": 0.2,
        "max_output_tokens": 8192,
    },
    tools=[FunctionTool(deal_advisory), FunctionTool(wall_crossing)],
)

# Carried by the design and NOT by this module. Nothing below is
# enforced by loading this agent into ADK:
#   - mandate: may decide ['advise_deal', 'cross_the_wall']
#   - 3 resolved permissions
#   - 2 tool(s) requiring approval (ADK has no approval gate on a FunctionTool): ['deal_advisory', 'wall_crossing']
#   - 5 guardrail(s) — expressible as before_/after_ callbacks in host code, never in this module
#   - placement 'banking--deal_room' and its network policy
#   - sandbox 'deal_room': network none
#   - 2 paired human(s), including the approver an approval would route to

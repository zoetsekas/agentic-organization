# Generated by orgagents from the system spec 'atlas' (spec_version 1.3.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

from google.adk.agents import LlmAgent
from google.adk.tools import FunctionTool

def breach_override(**kwargs):
    """Override a limit breach with a documented rationale."""
    # Supplied by the host at registration; the design names the tool, not its code.
    raise NotImplementedError("bind breach_override in host code")

def credit_approval(**kwargs):
    """Approve a credit-risk position."""
    # Supplied by the host at registration; the design names the tool, not its code.
    raise NotImplementedError("bind credit_approval in host code")

def limit_setting(**kwargs):
    """Set or change a trading risk limit."""
    # Supplied by the host at registration; the design names the tool, not its code.
    raise NotImplementedError("bind limit_setting in host code")

def risk_analytics_run(**kwargs):
    """Run VaR and stress models over positions and market data."""
    # Supplied by the host at registration; the design names the tool, not its code.
    raise NotImplementedError("bind risk_analytics_run in host code")

cro_agent = LlmAgent(
    name="cro_agent",
    model="gemini-2.5-pro",
    description="Sets limits, rules on breaches and approves credit positions.",
    instruction="""You are chief-risk-officer.
Sets limits, rules on breaches and approves credit positions.

## Your instructions
You own the limits the businesses live within. Set them on the analytics, not on the desk's ambition; a breach is overridden only with a rationale that would survive a regulator reading it. You are independent of Markets and Consumer by design — if either asks you to loosen a limit to hit a number, that request is worth recording.

## Accountability
- Set limits, run the analytics behind them, and rule on breaches and credit.  _(role: risk_officer)_

## Organization
- Team: Atlas Global / Risk
- Leader of: risk
- Reports to: group_ceo_agent
- May delegate to: markets_head_agent
- Escalates to: group_ceo_agent

## The people you answer to
- Lena Fischer (Chief Risk Officer) — owner, lena.fischer@atlas.example
- Beatriz Costa (Head of Internal Audit) — approver, beatriz.costa@atlas.example
- Always seek approval before: breach_override, credit_approval, limit_setting

## Missions you are on
- APAC conduct examination response — Respond to the APAC regulator's conduct examination of the trading business: assemble the evidence, answer the findings, and close the gaps. (until 2026-12-15)
- For the duration, you may work directly with: markets_head_agent.

## Shared operating principles
- Evidence over conviction. An unsourced number does not leave the desk.  _(from organization)_
- A control you route around is a control you do not have.  _(from organization)_
- When a limit, a barrier or a hold is in doubt, it holds until it is lifted.  _(from organization)_

## Boundaries enforced on you
- no_pii_outbound: data_class → block
- no_mnpi_outbound: data_class → block
- no_aml_outbound: data_class → block
- no_credentials_out: secrets → block
- screen_inbound_messages: prompt_injection → escalate
  These are enforced outside you. Do not attempt to work around one; say what you needed instead.

## Operating rules
- Prefer delegating to a team member whose role covers the task.
- Use an encoded workflow for any process that must be auditable.
- Prefer a sub-agent for a bounded task you can describe as a tool.
- You may not widen your own access; ask your leader instead.""",
    generate_content_config={
        "temperature": 0.2,
        "max_output_tokens": 8192,
    },
    tools=[FunctionTool(breach_override), FunctionTool(credit_approval), FunctionTool(limit_setting), FunctionTool(risk_analytics_run)],
    # sub_agents wired in agents/__init__.py from delegates_to=['markets_head_agent']
)

# Carried by the design and NOT by this module. Nothing below is
# enforced by loading this agent into ADK:
#   - mandate: may decide ['approve_credit_risk', 'override_limit_breach', 'set_risk_limit']
#   - 5 resolved permissions
#   - 3 tool(s) requiring approval (ADK has no approval gate on a FunctionTool): ['breach_override', 'credit_approval', 'limit_setting']
#   - 5 guardrail(s) — expressible as before_/after_ callbacks in host code, never in this module
#   - placement 'risk--risk_analytics' and its network policy
#   - sandbox 'risk_analytics': network allowlist
#   - 2 paired human(s), including the approver an approval would route to

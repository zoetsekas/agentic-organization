# Generated by orgagents from the system spec 'atlas' (spec_version 1.4.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_service_account" "group_ceo_agent" {
  # Workload identity for agent 'group_ceo_agent' (ADR-0015)
  account_id   = "agent-group_ceo_agent"
  display_name = "group-chief-executive workload identity"
}

resource "google_project_iam_member" "group_ceo_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.group_ceo_agent.email}"
}

resource "google_service_account" "consumer_head_agent" {
  # Workload identity for agent 'consumer_head_agent' (ADR-0015)
  account_id   = "agent-consumer_head_agent"
  display_name = "head-consumer workload identity"
}

resource "google_project_iam_member" "consumer_head_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.consumer_head_agent.email}"
}

resource "google_service_account" "loan_officer_agent" {
  # Workload identity for agent 'loan_officer_agent' (ADR-0015)
  account_id   = "agent-loan_officer_agent"
  display_name = "loan-officer workload identity"
}

resource "google_project_iam_member" "loan_officer_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.loan_officer_agent.email}"
}

resource "google_project_iam_member" "loan_officer_agent_roles_editor" {
  # write:capability:loan_origination
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.loan_officer_agent.email}"
}

resource "google_project_iam_member" "loan_officer_agent_roles_bigquery_dataviewer" {
  # query:capability:customer_lookup
  project = var.project
  role    = "roles/bigquery.dataViewer"
  member  = "serviceAccount:${google_service_account.loan_officer_agent.email}"
}

resource "google_service_account" "cib_head_agent" {
  # Workload identity for agent 'cib_head_agent' (ADR-0015)
  account_id   = "agent-cib_head_agent"
  display_name = "head-cib workload identity"
}

resource "google_project_iam_member" "cib_head_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.cib_head_agent.email}"
}

resource "google_service_account" "markets_head_agent" {
  # Workload identity for agent 'markets_head_agent' (ADR-0015)
  account_id   = "agent-markets_head_agent"
  display_name = "head-markets workload identity"
}

resource "google_project_iam_member" "markets_head_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.markets_head_agent.email}"
}

resource "google_service_account" "trader_agent" {
  # Workload identity for agent 'trader_agent' (ADR-0015)
  account_id   = "agent-trader_agent"
  display_name = "trader workload identity"
}

resource "google_project_iam_member" "trader_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.trader_agent.email}"
}

resource "google_project_iam_member" "trader_agent_roles_editor" {
  # write:capability:trade_execution
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.trader_agent.email}"
}

resource "google_project_iam_member" "trader_agent_roles_bigquery_dataviewer" {
  # query:capability:position_query
  project = var.project
  role    = "roles/bigquery.dataViewer"
  member  = "serviceAccount:${google_service_account.trader_agent.email}"
}

resource "google_service_account" "research_analyst_agent" {
  # Workload identity for agent 'research_analyst_agent' (ADR-0015)
  account_id   = "agent-research_analyst_agent"
  display_name = "research-analyst workload identity"
}

resource "google_project_iam_member" "research_analyst_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.research_analyst_agent.email}"
}

resource "google_project_iam_member" "research_analyst_agent_roles_pubsub_publisher" {
  # publish:capability:research_publishing
  project = var.project
  role    = "roles/pubsub.publisher"
  member  = "serviceAccount:${google_service_account.research_analyst_agent.email}"
}

resource "google_service_account" "banker_agent" {
  # Workload identity for agent 'banker_agent' (ADR-0015)
  account_id   = "agent-banker_agent"
  display_name = "investment-banker workload identity"
}

resource "google_project_iam_member" "banker_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.banker_agent.email}"
}

resource "google_project_iam_member" "banker_agent_roles_editor" {
  # write:capability:deal_advisory
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.banker_agent.email}"
}

resource "google_service_account" "cro_agent" {
  # Workload identity for agent 'cro_agent' (ADR-0015)
  account_id   = "agent-cro_agent"
  display_name = "chief-risk-officer workload identity"
}

resource "google_project_iam_member" "cro_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.cro_agent.email}"
}

resource "google_project_iam_member" "cro_agent_roles_editor" {
  # write:capability:limit_setting
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.cro_agent.email}"
}

resource "google_project_iam_member" "cro_agent_roles_bigquery_dataviewer" {
  # query:capability:risk_analytics_run
  project = var.project
  role    = "roles/bigquery.dataViewer"
  member  = "serviceAccount:${google_service_account.cro_agent.email}"
}

resource "google_service_account" "aml_agent" {
  # Workload identity for agent 'aml_agent' (ADR-0015)
  account_id   = "agent-aml_agent"
  display_name = "financial-crime-investigator workload identity"
}

resource "google_project_iam_member" "aml_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.aml_agent.email}"
}

resource "google_project_iam_member" "aml_agent_roles_editor" {
  # write:capability:aml_triage
  project = var.project
  role    = "roles/editor"
  member  = "serviceAccount:${google_service_account.aml_agent.email}"
}

resource "google_project_iam_member" "aml_agent_roles_bigquery_dataviewer" {
  # query:capability:transaction_trace
  project = var.project
  role    = "roles/bigquery.dataViewer"
  member  = "serviceAccount:${google_service_account.aml_agent.email}"
}

resource "google_service_account" "audit_agent" {
  # Workload identity for agent 'audit_agent' (ADR-0015)
  account_id   = "agent-audit_agent"
  display_name = "internal-auditor workload identity"
}

resource "google_project_iam_member" "audit_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.audit_agent.email}"
}

resource "google_project_iam_member" "audit_agent_roles_bigquery_dataviewer" {
  # query:capability:audit_testing
  project = var.project
  role    = "roles/bigquery.dataViewer"
  member  = "serviceAccount:${google_service_account.audit_agent.email}"
}

resource "google_service_account" "platform_engineer_agent" {
  # Workload identity for agent 'platform_engineer_agent' (ADR-0015)
  account_id   = "agent-platform_engineer_agent"
  display_name = "platform-engineer workload identity"
}

resource "google_project_iam_member" "platform_engineer_agent_roles_viewer" {
  # read:data_class:public_knowledge
  project = var.project
  role    = "roles/viewer"
  member  = "serviceAccount:${google_service_account.platform_engineer_agent.email}"
}

resource "google_project_iam_member" "platform_engineer_agent_roles_owner" {
  # administer:capability:production_deploy
  project = var.project
  role    = "roles/owner"
  member  = "serviceAccount:${google_service_account.platform_engineer_agent.email}"
}

# Generated by orgagents from the system spec 'atlas' (spec_version 1.4.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_cloud_run_v2_service" "group_ceo_agent" {
  name     = "agent-group_ceo_agent"
  region = var.region

  # team: Atlas Global
  # reports to: no one
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.group_ceo_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "group_ceo_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "atlas"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "consumer_head_agent" {
  name     = "agent-consumer_head_agent"
  region = var.region

  # team: Atlas Global / Consumer Bank
  # reports to: group_ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.consumer_head_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "consumer_head_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "consumer_bank"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "loan_officer_agent" {
  name     = "agent-loan_officer_agent"
  region = var.region

  # team: Atlas Global / Consumer Bank / Retail Lending
  # reports to: consumer_head_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.loan_officer_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "loan_officer_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "retail_lending"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "loan_officer_agent_sandbox" {
  # Execution environment 'banking_ops' — tier medium, network allowlist,
  # timeout 300s, mounts: account_data, customer_pii
  name     = "env-loan_officer_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.atlas_vpc.id
          subnetwork = google_compute_subnetwork.retail_lending__banking_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "cib_head_agent" {
  name     = "agent-cib_head_agent"
  region = var.region

  # team: Atlas Global / Corporate and Investment Bank
  # reports to: group_ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.cib_head_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "cib_head_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "cib"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "markets_head_agent" {
  name     = "agent-markets_head_agent"
  region = var.region

  # team: Atlas Global / Corporate and Investment Bank / Markets
  # reports to: cib_head_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.markets_head_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "0.25"
          memory = "512Mi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "markets_head_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "markets"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_service" "trader_agent" {
  name     = "agent-trader_agent"
  region = var.region

  # team: Atlas Global / Corporate and Investment Bank / Markets / Trading Desk
  # reports to: markets_head_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.trader_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "4"
          memory = "16Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "trader_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "trading"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "trader_agent_sandbox" {
  # Execution environment 'trading_floor' — tier large, network allowlist,
  # timeout 300s, mounts: trade_data, market_data
  name     = "env-trader_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.atlas_vpc.id
          subnetwork = google_compute_subnetwork.trading__trading_floor.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "research_analyst_agent" {
  name     = "agent-research_analyst_agent"
  region = var.region

  # team: Atlas Global / Corporate and Investment Bank / Markets / Research
  # reports to: markets_head_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.research_analyst_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "research_analyst_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "research"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "research_analyst_agent_sandbox" {
  # Execution environment 'research_desk' — tier medium, network allowlist,
  # timeout 300s, mounts: research_output, market_data
  name     = "env-research_analyst_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.atlas_vpc.id
          subnetwork = google_compute_subnetwork.research__research_desk.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "banker_agent" {
  name     = "agent-banker_agent"
  region = var.region

  # team: Atlas Global / Corporate and Investment Bank / Investment Banking
  # reports to: cib_head_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.banker_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "banker_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "banking"
    "network-posture" = "none"
  })
}

resource "google_cloud_run_v2_job" "banker_agent_sandbox" {
  # Execution environment 'deal_room' — tier medium, network none,
  # timeout 300s, mounts: mnpi
  name     = "env-banker_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.atlas_vpc.id
          subnetwork = google_compute_subnetwork.banking__deal_room.id
        }
        egress = "PRIVATE_RANGES_ONLY"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "cro_agent" {
  name     = "agent-cro_agent"
  region = var.region

  # team: Atlas Global / Risk
  # reports to: group_ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.cro_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "4"
          memory = "16Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "cro_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "risk"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "cro_agent_sandbox" {
  # Execution environment 'risk_analytics' — tier large, network allowlist,
  # timeout 300s, mounts: risk_models, market_data, trade_data
  name     = "env-cro_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.atlas_vpc.id
          subnetwork = google_compute_subnetwork.risk__risk_analytics.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "aml_agent" {
  name     = "agent-aml_agent"
  region = var.region

  # team: Atlas Global / Financial Crime
  # reports to: group_ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.aml_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "aml_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "financial_crime"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "aml_agent_sandbox" {
  # Execution environment 'banking_ops' — tier medium, network allowlist,
  # timeout 300s, mounts: account_data, customer_pii
  name     = "env-aml_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.atlas_vpc.id
          subnetwork = google_compute_subnetwork.financial_crime__banking_ops.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "audit_agent" {
  name     = "agent-audit_agent"
  region = var.region

  # team: Atlas Global / Internal Audit
  # reports to: group_ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.audit_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "2"
          memory = "8Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "audit_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "internal_audit"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "audit_agent_sandbox" {
  # Execution environment 'audit_room' — tier medium, network allowlist,
  # timeout 300s, mounts: audit_workpapers
  name     = "env-audit_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.atlas_vpc.id
          subnetwork = google_compute_subnetwork.internal_audit__audit_room.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

resource "google_cloud_run_v2_service" "platform_engineer_agent" {
  name     = "agent-platform_engineer_agent"
  region = var.region

  # team: Atlas Global / Technology
  # reports to: group_ceo_agent
  # runtime adapter: langchain_deepagents
  # max_parallel_subagents bounds one leader's fan-out inside one process and
  # is a different ceiling from the one below (ADR-0095 rule 5).
  # scale: scales to zero, at most 3 instance(s) × 1 session(s) = 3 concurrent (PLATFORM DEFAULT — no scaling was declared for this agent)
  scaling {
    min_instance_count = 0
    max_instance_count = 3
    max_instance_request_concurrency = 1
  }
  template {
    service_account = google_service_account.platform_engineer_agent.email
    containers {
      image = var.image
      resources {
        limits = {
          cpu    = "4"
          memory = "16Gi"
        }
      }
      env {
        name  = "ORGAGENTS_AGENT_ID"
        value = "platform_engineer_agent"
      }
    }
  }

  labels = merge(local.labels, {
    "team"            = "technology"
    "network-posture" = "allowlist"
  })
}

resource "google_cloud_run_v2_job" "platform_engineer_agent_sandbox" {
  # Execution environment 'build' — tier large, network allowlist,
  # timeout 300s, mounts: public_knowledge
  name     = "env-platform_engineer_agent"
  region = var.region
  template {
    template {
      vpc_access {
        network_interfaces {
          network    = google_compute_network.atlas_vpc.id
          subnetwork = google_compute_subnetwork.technology__build.id
        }
        egress = "ALL_TRAFFIC"
      }
    }
  }
}

# Generated by orgagents from the system spec 'atlas' (spec_version 1.4.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_cloud_run_v2_service" "risk_desk" {
  # Where the morning risk pack and limit alerts land for humans.
  # provider=google_chat purposes=notify
  # sla=none out_of_hours=queue
  # escalation: none
  name             = "channel-risk_desk"
  region = var.region
}

resource "google_secret_manager_secret" "risk_desk_credential" {
  # Workspace credential for 'risk_desk', referenced by name only.
  secret_id = "CHAT_BOT_TOKEN"
}

resource "google_cloud_run_v2_service" "control_room_escalations" {
  # Where a screened message or a barrier question goes for a human.
  # provider=google_chat purposes=ask, notify
  # sla=none out_of_hours=queue
  # escalation: none
  name             = "channel-control_room_escalations"
  region = var.region
}

resource "google_secret_manager_secret" "control_room_escalations_credential" {
  # Workspace credential for 'control_room_escalations', referenced by name only.
  secret_id = "CHAT_BOT_TOKEN"
}

resource "google_firestore_database" "long_term_memory" {
  # Long-term agent memory (ADR-0028); namespaces: control_lessons
  # retention: 365 days,
  # promotion from a session: True
  name             = "atlas-memory"
  region = var.region
}

resource "google_discovery_engine_data_store" "policy_library" {
  # Grounding source 'policy_library' (document_store); citation required: True
  # data classes: public_knowledge
  name             = "knowledge-policy_library"
  region = var.region
}

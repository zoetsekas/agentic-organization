# Generated by orgagents from the system spec 'atlas' (spec_version 1.3.0, ir 1.0.0).
# Do not edit: this file is regenerated on every compile. Put customizations in overlays/.

resource "google_compute_network" "atlas_vpc" {
  name                    = "vpc"
  auto_create_subnetworks = false
  # One VPC for the whole system. Every placement gets its own
  # subnet below, and nothing crosses without a firewall rule the
  # design asked for (ADR-0084).
}

resource "google_compute_subnetwork" "banking__deal_room" {
  name          = "subnet-banking--deal_room"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.8.0.0/24"
  # placement 'banking--deal_room' -- unit 'banking', sandbox 'deal_room',
  # posture 'none', agents: banker_agent
}

resource "google_compute_subnetwork" "financial_crime__banking_ops" {
  name          = "subnet-financial_crime--banking_ops"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.9.0.0/24"
  # placement 'financial_crime--banking_ops' -- unit 'financial_crime', sandbox 'banking_ops',
  # posture 'allowlist', agents: aml_agent
}

resource "google_compute_subnetwork" "financial_crime__fincrime_enclave" {
  name          = "subnet-financial_crime--fincrime_enclave"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.10.0.0/24"
  # placement 'financial_crime--fincrime_enclave' -- unit 'financial_crime', sandbox 'fincrime_enclave',
  # posture 'none', agents: aml_agent
}

resource "google_compute_subnetwork" "internal_audit__audit_room" {
  name          = "subnet-internal_audit--audit_room"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.11.0.0/24"
  # placement 'internal_audit--audit_room' -- unit 'internal_audit', sandbox 'audit_room',
  # posture 'allowlist', agents: audit_agent
}

resource "google_compute_subnetwork" "research__research_desk" {
  name          = "subnet-research--research_desk"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.12.0.0/24"
  # placement 'research--research_desk' -- unit 'research', sandbox 'research_desk',
  # posture 'allowlist', agents: research_analyst_agent
}

resource "google_compute_subnetwork" "retail_lending__banking_ops" {
  name          = "subnet-retail_lending--banking_ops"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.13.0.0/24"
  # placement 'retail_lending--banking_ops' -- unit 'retail_lending', sandbox 'banking_ops',
  # posture 'allowlist', agents: loan_officer_agent
}

resource "google_compute_subnetwork" "retail_lending__clean_room" {
  name          = "subnet-retail_lending--clean_room"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.14.0.0/24"
  # placement 'retail_lending--clean_room' -- unit 'retail_lending', sandbox 'clean_room',
  # posture 'none', agents: loan_officer_agent
}

resource "google_compute_subnetwork" "risk__risk_analytics" {
  name          = "subnet-risk--risk_analytics"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.15.0.0/24"
  # placement 'risk--risk_analytics' -- unit 'risk', sandbox 'risk_analytics',
  # posture 'allowlist', agents: cro_agent
}

resource "google_compute_subnetwork" "technology__build" {
  name          = "subnet-technology--build"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.16.0.0/24"
  # placement 'technology--build' -- unit 'technology', sandbox 'build',
  # posture 'allowlist', agents: platform_engineer_agent
}

resource "google_compute_subnetwork" "technology__deploy_runner" {
  name          = "subnet-technology--deploy_runner"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.17.0.0/24"
  # placement 'technology--deploy_runner' -- unit 'technology', sandbox 'deploy_runner',
  # posture 'allowlist', agents: platform_engineer_agent
}

resource "google_compute_subnetwork" "trading__trading_floor" {
  name          = "subnet-trading--trading_floor"
  region = var.region
  network       = google_compute_network.atlas_vpc.id
  ip_cidr_range = "10.18.0.0/24"
  # placement 'trading--trading_floor' -- unit 'trading', sandbox 'trading_floor',
  # posture 'allowlist', agents: trader_agent
}

resource "google_compute_firewall" "deny_cross_placement" {
  name      = "deny-cross-placement"
  network   = google_compute_network.atlas_vpc.id
  priority  = 65534
  direction = "INGRESS"
  # Default-deny between placements (ADR-0069 rule 7). Only the
  # allow rules below open a path.
  deny { protocol = "all" }
  source_ranges = ["10.0.0.0/8"]
}

resource "google_compute_firewall" "allow_within_banking__deal_room" {
  name      = "allow-within-banking--deal_room"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'banking--deal_room' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.banker_agent.email]
  target_service_accounts = [google_service_account.banker_agent.email]
}

resource "google_compute_firewall" "allow_within_financial_crime__banking_ops" {
  name      = "allow-within-financial_crime--banking_ops"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'financial_crime--banking_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.aml_agent.email]
  target_service_accounts = [google_service_account.aml_agent.email]
}

resource "google_compute_firewall" "allow_within_financial_crime__fincrime_enclave" {
  name      = "allow-within-financial_crime--fincrime_enclave"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'financial_crime--fincrime_enclave' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.aml_agent.email]
  target_service_accounts = [google_service_account.aml_agent.email]
}

resource "google_compute_firewall" "allow_within_internal_audit__audit_room" {
  name      = "allow-within-internal_audit--audit_room"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'internal_audit--audit_room' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.audit_agent.email]
  target_service_accounts = [google_service_account.audit_agent.email]
}

resource "google_compute_firewall" "allow_within_research__research_desk" {
  name      = "allow-within-research--research_desk"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'research--research_desk' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.research_analyst_agent.email]
  target_service_accounts = [google_service_account.research_analyst_agent.email]
}

resource "google_compute_firewall" "allow_within_retail_lending__banking_ops" {
  name      = "allow-within-retail_lending--banking_ops"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'retail_lending--banking_ops' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.loan_officer_agent.email]
  target_service_accounts = [google_service_account.loan_officer_agent.email]
}

resource "google_compute_firewall" "allow_within_retail_lending__clean_room" {
  name      = "allow-within-retail_lending--clean_room"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'retail_lending--clean_room' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.loan_officer_agent.email]
  target_service_accounts = [google_service_account.loan_officer_agent.email]
}

resource "google_compute_firewall" "allow_within_risk__risk_analytics" {
  name      = "allow-within-risk--risk_analytics"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'risk--risk_analytics' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.cro_agent.email]
  target_service_accounts = [google_service_account.cro_agent.email]
}

resource "google_compute_firewall" "allow_within_technology__build" {
  name      = "allow-within-technology--build"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'technology--build' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.platform_engineer_agent.email]
  target_service_accounts = [google_service_account.platform_engineer_agent.email]
}

resource "google_compute_firewall" "allow_within_technology__deploy_runner" {
  name      = "allow-within-technology--deploy_runner"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'technology--deploy_runner' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.platform_engineer_agent.email]
  target_service_accounts = [google_service_account.platform_engineer_agent.email]
}

resource "google_compute_firewall" "allow_within_trading__trading_floor" {
  name      = "allow-within-trading--trading_floor"
  network   = google_compute_network.atlas_vpc.id
  priority  = 1000
  direction = "INGRESS"
  # Traffic inside placement 'trading--trading_floor' is permitted and unlisted (ADR-0069).
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.trader_agent.email]
  target_service_accounts = [google_service_account.trader_agent.email]
}

resource "google_compute_firewall" "allow_0_financial_crime__banking_ops_to_internal_audit__audit_room" {
  name      = "allow-0"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "INGRESS"
  # both are on channel 'control_room_escalations' (via channel:control_room_escalations).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.aml_agent.email]
  target_service_accounts = [google_service_account.audit_agent.email]
}

resource "google_compute_firewall" "allow_1_financial_crime__fincrime_enclave_to_internal_audit__audit_room" {
  name      = "allow-1"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "INGRESS"
  # both are on channel 'control_room_escalations' (via channel:control_room_escalations).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.aml_agent.email]
  target_service_accounts = [google_service_account.audit_agent.email]
}

resource "google_compute_firewall" "allow_2_internal_audit__audit_room_to_financial_crime__banking_ops" {
  name      = "allow-2"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "INGRESS"
  # both are on channel 'control_room_escalations' (via channel:control_room_escalations).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.audit_agent.email]
  target_service_accounts = [google_service_account.aml_agent.email]
}

resource "google_compute_firewall" "allow_3_internal_audit__audit_room_to_financial_crime__fincrime_enclave" {
  name      = "allow-3"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "INGRESS"
  # both are on channel 'control_room_escalations' (via channel:control_room_escalations).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.audit_agent.email]
  target_service_accounts = [google_service_account.aml_agent.email]
}

resource "google_compute_firewall" "allow_4_internal_audit__audit_room_to_risk__risk_analytics" {
  name      = "allow-4"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared consult flow (via flow:audit_agent->cro_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.audit_agent.email]
  target_service_accounts = [google_service_account.cro_agent.email]
}

resource "google_compute_firewall" "allow_5_retail_lending__banking_ops_to_risk__risk_analytics" {
  name      = "allow-5"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared consult flow (via flow:loan_officer_agent->cro_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.loan_officer_agent.email]
  target_service_accounts = [google_service_account.cro_agent.email]
}

resource "google_compute_firewall" "allow_6_retail_lending__clean_room_to_risk__risk_analytics" {
  name      = "allow-6"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared consult flow (via flow:loan_officer_agent->cro_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.loan_officer_agent.email]
  target_service_accounts = [google_service_account.cro_agent.email]
}

resource "google_compute_firewall" "allow_7_trading__trading_floor_to_risk__risk_analytics" {
  name      = "allow-7"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "INGRESS"
  # a declared escalate flow (via flow:trader_agent->cro_agent).
  # A declared, official path (ADR-0069 rule 7); without it
  # the default-deny above stands.
  allow { protocol = "tcp" }
  source_service_accounts = [google_service_account.trader_agent.email]
  target_service_accounts = [google_service_account.cro_agent.email]
}

resource "google_compute_firewall" "deny_egress_banking__deal_room" {
  name      = "deny-egress-banking--deal_room"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "EGRESS"
  # sandbox 'deal_room' posture 'none': its agents reach
  # nothing outbound -- PII, MNPI or a live sample never calls home.
  deny { protocol = "all" }
  destination_ranges = ["0.0.0.0/0"]
  target_service_accounts = [google_service_account.banker_agent.email]
}

resource "google_compute_firewall" "deny_egress_financial_crime__fincrime_enclave" {
  name      = "deny-egress-financial_crime--fincrime_enclave"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "EGRESS"
  # sandbox 'fincrime_enclave' posture 'none': its agents reach
  # nothing outbound -- PII, MNPI or a live sample never calls home.
  deny { protocol = "all" }
  destination_ranges = ["0.0.0.0/0"]
  target_service_accounts = [google_service_account.aml_agent.email]
}

resource "google_compute_firewall" "deny_egress_retail_lending__clean_room" {
  name      = "deny-egress-retail_lending--clean_room"
  network   = google_compute_network.atlas_vpc.id
  priority  = 900
  direction = "EGRESS"
  # sandbox 'clean_room' posture 'none': its agents reach
  # nothing outbound -- PII, MNPI or a live sample never calls home.
  deny { protocol = "all" }
  destination_ranges = ["0.0.0.0/0"]
  target_service_accounts = [google_service_account.loan_officer_agent.email]
}

# NOTE: sandboxes audit_room, banking_ops, build, deploy_runner, research_desk, risk_analytics, trading_floor declare a
# hostname egress allowlist. A firewall rule works on IP
# ranges and service accounts, not FQDNs, so the allowlist is
# enforced by an egress proxy / Cloud NAT with an FQDN policy,
# wired in overlays/ -- not by these rules (ADR-0084).
